<!DOCTYPE html>
<html id="html">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Confledis{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link href="{{ asset('pages/css/base.css') }}" rel="stylesheet"/>
        <!-- Pour le responsive -->
        <meta name="viewport" content="width=device-width, minimum-scale=0.8, initial-scale=0.8, user-scalable=0, interactive-widget=resizes-content">
        {% block stylesheets %}{{ encore_entry_link_tags('app') }}{% endblock %}
            
        {% block javascripts %}{{ encore_entry_script_tags('app') }}{% endblock %}
    </head>

    <body>
        <main>
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
